# This file is committed — safe because it only contains the public API URL
# Vite injects this at build time via import.meta.env

# Set to your actual Render backend URL (no trailing slash)
VITE_API_BASE_URL=https://gitlab-handbook-ai-backend.onrender.com